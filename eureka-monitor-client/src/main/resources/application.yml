spring:
  application.name: service-turbine
server:
  port: 8769
security:
  basic:
    enabled: false
turbine:
  aggregator:
    clusterConfig: default   # ????????Щ???????????","???????default???????http://.../turbine.stream?cluster={clusterConfig??}????
  appConfig: eureka-feign-client,eureka-ribbon-client  # ????Eureka?е?serviceId?б??????????Щ????
  clusterNameExpression: new String("default")
  # 1. clusterNameExpression?????????????????appName???????turbine.aggregator.clusterConfig?????????????????????
  # 2. ??clusterNameExpression: default???turbine.aggregator.clusterConfig?????д???????????default
  # 3. ??clusterNameExpression: metadata['cluster']???????????????????????eureka.instance.metadata-map.cluster: ABC??????????????turbine.aggregator.clusterConfig: ABC
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/