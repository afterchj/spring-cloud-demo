#my:
#  foo: foo version 1
spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:8769
      fail-fast: true
#      discovery:
#        service-id: config-server
  profiles:
    active: dev
